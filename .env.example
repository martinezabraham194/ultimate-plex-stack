# .env.example - copy to .env and edit values before deploying
# NOTE: Do NOT commit your real .env with secrets to git. Use Docker secrets for sensitive values.

# User / permissions
PUID=1000            # Linux user id that owns config/media dirs
PGID=1000            # Linux group id
TZ=America/New_York  # Timezone

# Paths (absolute)
BASE_PATH=/media/abmartinez/Linux HDD/Plex   # base config path for containers
MEDIA_SHARE=/media/abmartinez/Linux HDD/Plex # media storage path (movies, tv, downloads)

# Host LAN IP (optional) - used by some helpers to call services on the host
SERVER_IP=192.168.1.10

# Plex
# If running Plex in Swarm is unsupported for your setup (HW transcoding / discovery),
# run Plex as a standalone container with host networking and keep the rest in Swarm.
PLEX_CLAIM=             # put claim token in Docker secret (see SECRETS_README.md) - leave blank if using secret

# VPN (gluetun / ProtonVPN)
# ProtonVPN credentials must be created as Docker secrets (see SECRETS_README.md)
PROTONVPN_REGION=us-free-01

# API keys (store sensitive keys as Docker secrets)
SONARR_KEY=
RADARR_KEY=
MEMBARR_TOKEN=

# UI / published host ports (change if these conflict on your host)
QB_WEBUI_PORT=31808
QB_TORRENT_PORT=8694

PLEX_PORT=32400
RADARR_PORT=7878
SONARR_PORT=8989
READARR_PORT=8787
LIDARR_PORT=8686
PROWLARR_PORT=9696
OVERSEERR_PORT=5055
FLARESOLVERR_PORT=8191
TAUTULLI_PORT=8181
TDARR_WEB_PORT=8265
TDARR_SERVER_PORT=8266
BAZARR_PORT=6767
DOZZLE_PORT=9999
WIZARR_PORT=5690
CROSSSEED_PORT=2468

# Optional tuning
UMASK=002
WEBUI_USER=              # optional webui basic auth user (implementer can wire via secrets/labels)
WEBUI_PASS=

# Deployment
STACK_NAME=ultimate-plex
